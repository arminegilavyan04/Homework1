/*int[] arr = {10,22,36,35,64,88,12};
int n = arr.Length;
int i = 0;
    while (i < n-1)
    {
        int temp = arr[i];
        arr[i] = arr[n-1];
        arr[n-1] = temp;
        i++;
        n--;
    }
for (int l = 0; l < arr.Length; l++)//here i used arr.length instead of n as n has already changed its value due to the subtraction
{
    Console.Write(arr[l] + " ");
}
*/